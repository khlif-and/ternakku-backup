@props(['title' => 'SURAT JALAN / IZIN MEMBAWA TERNAK', 'number'])

<div class="title-container">
    <div class="title-text">{{ $title }}</div>
    <div class="title-number">Nomor : {{ $number }}</div>
</div>